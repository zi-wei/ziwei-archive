# 星空重建约束与证据

## 范围

- 保留 ZiWei ARCHIVE 的页面, 内容, 苹果轮廓, 点击重聚和全站动态背景.
- 依据 2026-09-04 保存的 OpenAI Astra 客户端脚本恢复星点与运动机制.
- 核对参考地址: https://openai.com/zh-Hans-CN/index/gpt-6-astra/ . 将本适配视为非官方个人站实现.
- 将结果标记为个人站适配, 不宣称原站全量或像素级复刻. 2026-09-18 实时原页返回 Cloudflare 403, 无法取得同机位实时对照.
- 不引入原站文案, 品牌, 视频或第三方图像. 苹果图继续使用用户提供的现有资产.

## 一手证据

按 `D:/ao/1gpt/temp/astra-source-chunks/*.pretty.js` 的行号核对. 保留原件只读.

| 原始文件 | SHA-256 |
| --- | --- |
| 2jgpexr4u1zu_.js | 6d0a99409c24f64aa526903f97bc76d47df99312f78fd5f3185eec0de8375053 |
| 1uq8b6yni-tzr.js | 00afda575f7c5565e69fda728857b0d25907905b9e197106a9b4e9247f2bbd30 |
| 1rfzm7jt1igp4.js | f1d101d3cf1399fce054b6fc86d24b0f8de47250afa3ad41a946116c91c57ea7 |

- 依据 `1rfzm7jt1igp4:26-35, 886-917` 提取亚像素覆盖与星点片元着色器. 保留柔软核心, 稀疏光芒, 明暗和颜色能量计算.
- 依据 `1rfzm7jt1igp4:40-73` 提取有界分散运动. 速度 0.4-0.8, 振幅 0.035-0.12, 滚动视差 0.08-0.28. 禁止统一方向无限漂移.
- 依据 `1rfzm7jt1igp4:1681-1704` 提取聚合函数. 保留每粒子延迟, 混合缓动和弧线.
- 依据 `1rfzm7jt1igp4:1306-1318` 恢复偏向细星的尺寸分布, 少量高亮星, 独立闪烁相位.
- 依据 `0kyg3mclnp-m6:127-152` 分离滚动状态与位置阻尼. 使用阻尼 6/4, 散开阈值 0.375-1.1875, 倾斜峰值 -52deg.
- 依据 `0kyg3mclnp-m6:180-225` 使用正交投影, 世界高度 10.9/12.7, 分散空间为可见范围的 1.12 倍.
- 依据 `1uq8b6yni-tzr:819-820, 867-902` 核对正交相机, 加法混合, bloom 与 ACES 色调映射管线.
- 依据 `1rfzm7jt1igp4:600-648` 保留 72% 侧向分流, 28% 中部散射, 开场全屏星场和 12% 常驻背景星.
- 不把旧笔记中的 5.5 秒和粒子总数当作已验证的原站默认配置.

## 登记适配差异

| 原站 | 本站实现 | 原因 | 重新考虑条件 |
| --- | --- | --- | --- |
| Three.js, 多条 SVG 轨道和数字造型 | 原生 WebGL, 从用户苹果图片采样, 添加浅层厚度 | 保留用户造型与无框架静态站 | 用户要求恢复原造型 |
| 完整摄影光学, 多级 bloom 和污镜效果 | 星点 pass, 半尺寸 bloom, 合成与色调映射; 不加污镜 | 限制阅读干扰和手机负载 | 实测需要更多光学层次 |
| 半浮点 HDR 缓冲与完整 ACES 管线 | RGBA8 缓冲与紧凑 ACES 拟合 | 不增加扩展依赖, 控制内存; 高亮能量会有截断, 不声称像素相等 | 取得完整光学配置并能进行实时对拍 |
| 外部场景配置决定密度, 颜色和时长 | 固定种子, 桌面 12000/手机 6800 粒子, 5 秒聚合, 白蓝为主少量暖星 | 原配置缺失, 保持个人站的克制氛围 | 取得完整配置 |
| 重播直接重置 intro | 先连续反向展开 0.8 秒, 再沿相同路径重聚 | 避免已成形苹果瞬间跳空 | 用户要求直接重启 |
| 完全让出正文中心的透明遮罩 | 用粒子侧向分流和已有正文底色保障阅读, 保留中心少量星光 | 用户明确要求下滑及其他页始终能看到星空 | 阅读实测表明仍受干扰 |
| 原页滚动布局 | 将当前 hero 有效滚动轨道映射至 0-1.1875 | 保留原个人站结构, 确保离开第一屏前散开完成 | 个人站布局改变 |
| 源着色器覆盖多种预设和 GPU 指针模拟 | 保留聚合/倾斜/分散分支, 使用时间阻尼微视差 | 当前任务仅使用一种动画, 限制资源消耗 | 用户新增预设 |

## 验收约束

- 从 dist 服务运行桌面 1440px 与手机 390px 浏览器检查.
- 检查全部页面的 HTTP, 导航, 图片, 溢出和控制台.
- 实际查看聚合, 中途倾斜, 分散, 文章列表和详情截图.
- 验证点击连续性, 上下滚动可逆, 后台暂停, reduced-motion 静止, WebGL context 恢复和视口重排.
- 运行 npm ci, npm run build, npm test, npm run check, git diff --check.
- 通过后检查暂存内容, 提交, 推送并验证 GitHub Actions 与公网.

## 2026-09-18 本地验收记录

- 保留 npm ci, build, test, check 与 diff 检查结果. 浏览器主套件 16 项通过, 文章构建契约 1 项通过, 星空生命周期 5 项通过.
- 查阅 `D:/ao/1gpt/temp/astra-rebuild-20260918/local/` 的桌面/手机首页, 倾斜中段, 分散, 列表和详情截图. 以该目录作为本站实测记录, 不将其当作原站基线.
- 用最终合成 pass 的同步 readPixels 检查星点像素与动态变化. 不打开 preserveDrawingBuffer. 将普通页面截图单独用于布局和视觉复核, 避免临时隐藏前景时的合成层残留污染静止断言.
- 保留开场点击亮度连续性回归: 原实现会将显现程度从 0.1 突增至 1; 当前实现从点击时显现程度平滑继续.
- 核对聚合 GLSL 与亚像素覆盖 GLSL 对归档原件的文本包含检查均为 true.
- 下一步仅执行提交后公网验证; 实时原站对拍仍受原站 403 限制, 不报告未经测量的还原百分比.
