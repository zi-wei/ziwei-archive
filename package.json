{
  "name": "ziwei-archive",
  "version": "1.0.0",
  "private": true,
  "description": "Static personal archive for zw.enener.com",
  "scripts": {
    "build": "node tools/build-site.cjs",
    "test": "node tests/site.test.cjs && node tests/articles.test.cjs && node tests/starfield-lifecycle.test.cjs",
    "serve": "node tools/server.cjs",
    "check": "node --check assets/js/site.js && node --check assets/js/apple-starfield.js && node --check tools/build-site.cjs && node --check tools/server.cjs"
  },
  "dependencies": {
    "markdown-it": "^14.1.0"
  },
  "devDependencies": {
    "playwright-core": "1.62.1"
  }
}
